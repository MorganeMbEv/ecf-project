{% set route_name = app.request.attributes.get('_route') %}

<ul class="navbar-nav me-auto mb-2 mb-lg-0">
	<li  class="nav-item">
		<a {% if route_name == "app_homepage" %} class="nav-link active" {% else %} class="nav-link" {% endif %} href="{{ path('app_homepage') }}">Accueil</a>
	</li>
	<li class="nav-item">
		<a {% if route_name == "app_menu_card" %} class="nav-link active" {% else %} class="nav-link" {% endif %} href="{{ path('app_menu_card') }}">La carte</a>
	</li>
	<li class="nav-item">
		<a {% if route_name == "app_register" %} class="nav-link active" {% else %} class="nav-link" {% endif %} href="{{ path('app_register') }}">Réservation</a>
	</li>
</ul>